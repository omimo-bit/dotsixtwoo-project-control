function getDashboard_(user){var projects=listRows_('PROJECTS'),tasks=listRows_('TASKS'),meetings=listRows_('MEETINGS'),budget=getBudget_(),mine=tasks.filter(function(t){return String(t.picEmail||'').toLowerCase()===String(user.email).toLowerCase()||String(t.pic||'').toLowerCase()===String(user.name).toLowerCase()}),today=new Date();today.setHours(0,0,0,0);var tkey=Utilities.formatDate(today,Session.getScriptTimeZone(),'yyyy-MM-dd');var overdue=tasks.filter(function(t){var d=t.deadline?new Date(t.deadline):null;return d&&d<today&&t.status!=='Completed'});var risky={};overdue.forEach(function(t){risky[t.projectId]=1});tasks.filter(function(t){return t.status==='Blocked'}).forEach(function(t){risky[t.projectId]=1});return{totalProjects:projects.length,myTasks:mine.filter(function(t){return t.status!=='Completed'}).length,overdueTasks:mine.filter(function(t){var d=t.deadline?new Date(t.deadline):null;return d&&d<today&&t.status!=='Completed'}).length,momPending:meetings.filter(function(m){return m.momStatus==='Pending'}).length,todayMeetings:meetings.filter(function(m){return m.date&&Utilities.formatDate(new Date(m.date),Session.getScriptTimeZone(),'yyyy-MM-dd')===tkey}).length,blockedTasks:tasks.filter(function(t){return t.status==='Blocked'}).length,budgetAlerts:budget.filter(function(b){return b.budgetStatus==='ALERT'||b.budgetStatus==='OVER BUDGET'}).length,atRiskProjects:Object.keys(risky).length}}
